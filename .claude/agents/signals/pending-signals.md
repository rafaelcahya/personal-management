# Pending Cross-Agent Signals

> This file is the central inbox for all cross-agent communication.
> Every agent reads this at kickoff. After handling a signal, mark it `[RESOLVED]` — never delete rows.

---

## How to Use

**Writing a signal:**

1. Append a new row to the relevant section below
2. Use the exact signal format defined in `shared-knowledge.md`
3. Set status to `[PENDING]`

**Resolving a signal:**

1. Change status from `[PENDING]` to `[RESOLVED: YYYY-MM-DD]`
2. Add a one-line note on what was done

---

## Signals: Tester → Frontend (id requests)

| Status                 | Date       | Signal                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ---------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| [RESOLVED: 2026-05-17] | 2026-05-13 | 🔖 id Request — Tester → Frontend · Component: LogoutButton (wherever rendered in inventory + trading layouts) · Missing IDs: `id="logoutBtn"` → the logout `<button>` element · Needed for: cypress/e2e/auth/logout.cy.js (32 failures) · Action: Add testId + register in app-constants.yaml under `test_ids.auth.logout_btn` · Done: Changed desktop sign-out button id from `userMenuSignOut_landingPage` to `logoutBtn` in Sidebar.jsx; updated app-constants.json `auth.logout_btn_el` and `auth.user_menu_signout` to `logoutBtn`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| [RESOLVED: 2026-05-17] | 2026-05-13 | 🔖 id Request — Tester → Frontend · Component: UserMenu (landing page) · Missing IDs: `id="userMenuTrigger_landingPage"` → the menu trigger element · Needed for: cypress/e2e/auth/logout.cy.js (6 failures) · Action: Add testId + register in app-constants.yaml under `test_ids.auth.user_menu_trigger` · Done: `id="userMenuTrigger_landingPage"` was already present in Sidebar.jsx (line 164) — no change needed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| [RESOLVED: 2026-05-17] | 2026-05-17 | 🔖 id Request — Tester → Frontend · Component: UpdateProductName (app/main/inventory/product-name/UpdateProductName.jsx) · Missing IDs: (1) `id="productNameField_errorMessage_updateDialog"` → the `<FormMessage>` for the product_name field (for inline 409 error display). (2) `id="submitUpdateProductNameBtn_productNamePage"` → the Update submit `<Button>`. (3) `id="cancelUpdateProductNameBtn_productNamePage"` → the Cancel `<DialogClose>` button. · Needed for: UI tests covering inline "Product name already exists" error on update. · Action: Add these id attributes and register them in app-constants.json under `test_ids.product_name.*` · Done: All 3 IDs added to UpdateProductName.jsx; IDs were already registered in app-constants.json                                                                                                                                                                                                                                                                                                                        |
| [RESOLVED: 2026-05-17] | 2026-05-17 | 🔖 id Request — Tester → Frontend · Component: DeleteProductName (app/main/inventory/product-name/DeleteProductName.jsx) · Missing IDs: (1) `id="deleteProductNameBtn_productNamePage"` → the `<AlertDialogTrigger>` Button (disabled when in use). (2) `id="deleteProductNameConfirmBtn_productNamePage"` → the `<AlertDialogAction>` confirm button. · Needed for: UI test for delete button disabled state when product_count > 0 (in-use guard). · Action: Add these id attributes and register them in app-constants.json under `test_ids.product_name.*` · Done: Both IDs added to DeleteProductName.jsx; IDs were already registered in app-constants.json                                                                                                                                                                                                                                                                                                                                                                                                                          |
| [RESOLVED: 2026-05-20] | 2026-05-20 | 🔖 id Request — Tester → Frontend · Component: OnboardingPage (app/main/running/onboarding/page.jsx) · Missing IDs on all form inputs — currently tests use placeholder-based selectors which are fragile. Missing IDs: (1) `id="birthDateInput_onboarding"` on `<Input type="date">` in StepBiometrics. (2) `id="heightInput_onboarding"` on Height input. (3) `id="weightInput_onboarding"` on Weight input. (4) `id="restingHrInput_onboarding"` on Resting HR input. (5) `id="maxHrManualInput_onboarding"` on manual max HR input. (6) `id="continueBtn_onboarding"` on Continue submit button. (7) `id="connectStravaBtn_onboarding"` on Connect with Strava button. (8) `id="skipStravaBtn_onboarding"` on Skip Strava button. (9) `id="finishBtn_onboarding"` on finish/Go to dashboard button. (10) `id="backBtn_onboarding"` on Back button shown on steps 2+. · Also added: maxHrFormulaRadio, maxHrManualRadio, maxHrFormulaPreview, goalYesRadio, goalNoRadio, targetDistanceInput, targetDateInput, rootError, stepCountLabel. · Done: All IDs added to onboarding/page.jsx. |

---

## Signals: Tester → Backend (endpoint gaps)

| Status                 | Date       | Signal                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ---------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [RESOLVED: 2026-05-17] | 2026-05-13 | ⚠️ Custom Command Gap — Tester → Frontend · Missing: `cy.getAuthToken()` not defined in cypress/support/commands.js — causes 9 failures across add/list/delete/update/favorite/stock/history/detail/summary product tests · Missing: `cy.logout()` not defined — causes 1 failure in product-detail-ui · Action: Define both commands in cypress/support/commands.js. `cy.getAuthToken()` should return bearer token from session; `cy.logout()` should clear session and redirect to /login · Done: `cy.getAuthToken()` was already defined in cypress/support/common/auth/ui-commands.js. Added `cy.logout()` command to the same file — clears localStorage, cookies, calls /api/auth/logout, then visits /login                     |
| [RESOLVED: 2026-05-20] | 2026-05-20 | ⚠️ Endpoint Gap — Tester → Backend · Endpoint: POST /api/running/v1/onboarding/biometric · Root cause: Zod v4 renamed `ZodError.errors` → `ZodError.issues`. Route handler accessed `parsed.error.errors[0].message` which threw TypeError (undefined[0]) caught by outer catch → 500. · Done: Fixed to `parsed.error.issues[0].message` in biometric/route.js. Now returns 400 correctly for all validation failures.                                                                                                                                                                                                                                                                                                                  |
| [RESOLVED: 2026-05-27] | 2026-05-27 | ⚠️ Bug — Tester → Backend · Endpoint: PATCH /api/running/v1/goals/:id · File: `lib/services/running/goals/updateGoal.js` · Root cause: Service uses `.single()` instead of `.maybeSingle()` on the Supabase query. When a non-existent or unowned goal ID is patched, PostgREST throws error PGRST116, which bubbles up as a 500 instead of a proper 404. · Fix: Change `.single()` → `.maybeSingle()`, check if result is null, return 404. · Impact: `activityDetailApi.cy.js` test `PATCH /goals/:id — 404 ownership` currently asserts `oneOf([404, 500])` — should be strict 404 after fix. · Done: Changed `.single()` to `.maybeSingle()` in updateGoal.js; route handler already had the `if (!goal)` null-check returning 404. |

---

## Signals: UI/UX → PM (UX gaps)

| Status | Date | Signal         |
| ------ | ---- | -------------- |
| —      | —    | No signals yet |

---

## Signals: UI/UX → Frontend (design handoff)

| Status                 | Date       | Signal                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ---------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [RESOLVED: 2026-05-26] | 2026-05-26 | 🎨 Design Handoff — UI/UX → Frontend [DONE: HrZonesChart.jsx and AIInsightCard.jsx implemented in activities/components/; both rendered in activities/[id]/page.jsx; test IDs and endpoints registered in app-constants.json] · Feature: Running Tracker — HR Zones Chart + AI Insight Card · Components: (1) HrZonesChart — horizontal bar chart per zone (Z1-Z5); data source: `activity.zones.heart_rate.zones[]` with `{min,max,time}`; zone colors: Z1=gray(#6b7280) Z2=blue(#3b82f6) Z3=amber(#f59e0b) Z4=orange(#f97316) Z5=red(#ef4444); labels on left "Z1 Recovery <115bpm" etc.; bar shows %time + duration "45% • 35m 00s"; empty state `data-testid="hrZonesEmpty"` shows "Zone data not available"; section root `data-testid="hrZonesSection"`; placed between stream charts and laps section. (2) AIInsightCard — card with purple-400 left border (`border-l-4 border-purple-400`); header: Sparkles icon + "AI Coach" text + "BETA" badge (`bg-purple-100 text-purple-700 text-xs`); 6 states: loading=skeleton 3 lines; empty=icon+"Belum ada analisis"+`aiInsightGenerateBtn` button; pending=spinner+"Sedang dianalisis..."+muted "Biasanya < 30 detik"+Refresh button `aiInsightPending`; content=rendered markdown sections in `aiInsightContent` (## headers as `font-semibold text-sm`, bullet lists, paragraph text); error=retry button `aiInsightRetry`; failed/invalid=silent (hide card entirely); placed at TOP of activity detail, before stream charts, after main stats grid; card root `data-testid="aiInsightCard"`, loading `aiInsightLoading`, empty `aiInsightEmpty`, generate btn `aiInsightGenerateBtn`, pending `aiInsightPending`, content `aiInsightContent`, error `aiInsightError`, retry `aiInsightRetry`. API: `fetchActivityInsight(activityId)` → GET /api/running/v1/ai/insights; `requestInsightGeneration(activityId)` → POST /api/running/v1/ai/insights/generate (both in lib/api/running.js). Insight shape: `{id, insight_type, status, is_valid, title, content, data_refs, acknowledged, created_at}`. Markdown rendering: split content by `## ` sections, render h2 as bold label, paragraphs as `<p>`, bullet lists as `<ul><li>`. · Ready to implement |

---

## Signals: Backend → Frontend (API contracts)

| Status                 | Date       | Signal                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ---------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [RESOLVED: 2026-05-17] | 2026-05-17 | 📡 API Contract — Backend → Frontend · Feature: Product Name — uniqueness checks + delete guard + product_count · Endpoints changed: (1) GET /api/inventory/v1/product-name/list — response now includes `product_count` per item (count of active products using that name). (2) POST /api/inventory/v1/product-name/create — now returns HTTP 409 `{ error: "CONFLICT", message: "Product name already exists" }` if name already exists (case-insensitive, excluding soft-deleted). (3) PUT /api/inventory/v1/product-name/update/[id] — same 409 behavior, but excludes the current record from the uniqueness check. (4) DELETE /api/inventory/v1/product-name/delete/[id] — now returns HTTP 409 `{ error: "CONFLICT", message: "Product name is still used by X product(s) and cannot be deleted" }` if still in use. · Action: (a) Use `product_count` to show warning box and disable delete button in Edit modal when `product_count > 0`. (b) Handle 409 responses from create/update to show "Product name already exists" form error. (c) Handle 409 from delete as a safety net toast error. · Ready to integrate: yes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| [RESOLVED: 2026-05-23] | 2026-05-23 | 📡 API Contract — Backend → Frontend · Feature: Running Tracker Dashboard · New endpoint: GET /api/running/v1/dashboard · Auth: required (401 if unauthenticated) · Response shape: `{ data: { weekly_stats: { current: { distance_m, duration_sec, count, avg_pace_sec_per_km }, prev: { distance_m, duration_sec, count, avg_pace_sec_per_km } }, training_load: { acwr, acute_load_7d, chronic_load_28d, status }, recent_activities: [{ id, started_at, distance_m, duration_sec, avg_pace_sec_per_km, avg_hr, activity_type, source }], calendar_activities: [{ date, activity_type }], health_today: { logged, data } } }` · Numeric notes: distance_m and duration_sec are integers (summed), avg_pace_sec_per_km is rounded integer or null (null when no distance), acwr/load values are floats rounded to 2 and 1 decimal places respectively, status is one of: no_data / low / optimal / caution / danger · Error cases: 401 Unauthorized, 500 Internal server error · Ready to integrate: yes · Frontend implemented dashboard page at app/main/running/(app)/dashboard/page.jsx                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| [RESOLVED: 2026-05-24] | 2026-05-24 | 📡 API Contract — Backend → Frontend · Feature: Running Tracker — Activities list, activity detail, performance trends (power + gear + polyline) · Endpoints changed (additive only, no breaking changes): (1) GET /api/running/v1/activities — response items now include additional fields: `moving_time_sec, name, elevation_gain_m, pr_count, workout_type, avg_cadence, avg_temp_c, calories, avg_watts, weighted_avg_watts, device_watts, summary_polyline, gear_id, gear_name, gear_distance_m`. `gear_name` and `gear_distance_m` are joined from `rt_gear` — null if no gear. (2) GET /api/running/v1/activities/[id] — response `activity` object now includes: `name, avg_watts, weighted_avg_watts, device_watts, summary_polyline, gear_id, avg_temp_c, pr_count, workout_type, relative_effort, max_hr, max_pace_sec_per_km`. New top-level field `gear` is `{ name, distance_m }` or `null`. (3) GET /api/running/v1/performance-trends — response items now include `avg_watts, weighted_avg_watts, device_watts`. · New API client functions in lib/api/running.js: `fetchActivities({ page, limit, type })` → returns paginated `{ data, total, page, limit }`; `fetchActivity(id)` → returns `{ activity, splits }`. · DB: new table `rt_gear` (id, user_id, name, brand_name, model_name, distance_m, retired, last_fetched_at). Strava sync now auto-fetches gear details and upserts into `rt_gear`. · Ready to integrate: yes · Done: ActivitySection.jsx updated (power + temp + gear line); PerformanceTrends.jsx got Power tab + PowerChart; RouteMap.jsx created; activities/page.jsx and activities/[id]/page.jsx fully implemented. |

---

| [RESOLVED: 2026-05-25] | 2026-05-25 | 📡 API Contract — Backend → Frontend · Feature: Running Tracker — Race Log + Goals update · New endpoints: (1) GET /api/running/v1/race-log — auth required; returns `{ data: [{ id, title, race_date, distance_m, finish_time_sec, avg_pace_sec_per_km, avg_hr, elevation_gain_m, position_overall, position_category, did_not_finish, activity_id, notes, created_at }], message }` — ordered by race_date DESC. (2) POST /api/running/v1/race-log — body: `{ title (required), race_date (required, YYYY-MM-DD, not future), distance_m (required, >0), finish_time_sec? (required unless did_not_finish=true), avg_hr?, elevation_gain_m?, position_overall?, position_category?, did_not_finish?, activity_id?, notes? }`; auth required; avg_pace_sec_per_km computed server-side; returns `{ data: <new entry>, message }` 201. (3) PATCH /api/running/v1/race-log/:id — body: same fields all optional (at least one required); auth + ownership check; avg_pace_sec_per_km recomputed if finish_time_sec or distance_m changes; returns `{ data: <updated entry>, message }` 200; 404 if not found/not owned. (4) DELETE /api/running/v1/race-log/:id — auth + ownership check; returns `{ message: "Deleted" }` 200; 404 if not found/not owned. (5) PATCH /api/running/v1/goals/:id — body: `{ title?, description?, target_date?, target_distance_m?, target_time_sec? }` at least one required; auth + ownership check; returns `{ data: <updated goal>, message }` 200; 404 if not found/not owned. · DB changes: `rt_goals` now has `title TEXT` and `description TEXT` columns. New table `rt_race_log` with RLS enabled. · Zod schema: `schemas/raceLog.js` exports `createRaceLogSchema`, `updateRaceLogSchema`, `updateGoalSchema`. · Fixture keys: `endpoints.running_race_log.*`, `endpoints.running_goals.update`, `test_ids.race_log.*`. · Error cases: 400 Validation failed, 401 Unauthorized, 404 Not found, 500 Internal server error. · Ready to integrate: yes |
| [RESOLVED: 2026-05-25] | 2026-05-25 | 📡 API Contract — Backend → Frontend · Feature: Running Tracker — Gear Management (list + update category/retirement_km) + Athlete shoe sync on Strava sync · New endpoints: (1) GET /api/running/v1/gear — no query params; auth required; returns `{ data: [{ id, name, brand_name, model_name, distance_m, retired, category, retirement_km, last_fetched_at }], message }` — ordered by retired asc then name asc. (2) PATCH /api/running/v1/gear — body: `{ id: string (required), category?: string|null, retirement_km?: integer|null }`; auth required; returns `{ data: <updated gear row>, message }`. · Error cases: 400 Validation failed (missing id or invalid fields), 401 Unauthorized, 500 Internal server error. · DB change: `rt_gear` table now has two new user-managed columns: `category TEXT DEFAULT NULL` and `retirement_km INTEGER DEFAULT NULL`. Strava sync NEVER overwrites these. · Strava sync change: POST /api/running/v1/auth/strava/callback now also calls `GET /api/v3/athlete` at start of each sync to proactively upsert all athlete shoes into `rt_gear` (not just shoes referenced by activities). · Schema: `schemas/runningGear.js` — exports `updateGearSchema` (Zod). · Fixture keys: `endpoints.running_gear.list`, `endpoints.running_gear.update`, `test_ids.running_gear.*` · Ready to integrate: yes · Done: fetchGear + updateGear added to lib/api/running.js; ShoeRotation.jsx created at app/main/running/(app)/dashboard/components/ShoeRotation.jsx; ShoeRotation rendered in dashboard/page.jsx after PerformanceTrends. |

| [RESOLVED: 2026-05-27] | 2026-05-26 | 📡 API Contract — Backend → Frontend · Feature: Running Tracker — Derived Metrics (Aerobic Decoupling + Efficiency Factor) · No new endpoints — additive fields only on existing response. · Change: GET /api/running/v1/activities/[id] — `activity` object now may include two new nullable fields: `aerobic_decoupling` (NUMERIC 5,2, % value e.g. 3.47 or -1.22) and `efficiency_factor` (NUMERIC 6,4, ratio e.g. 0.0183). Both are NULL when gate conditions not met (duration ≤ 20 min OR avg_hr null), OR when activity was synced before this migration. · Computed at ingest time (Inngest step `compute-derived-metrics` in stravaFetchStreams) — not on render. · Interpretation for UI: aerobic_decoupling < 5% → green badge "Good"; 5–10% → amber badge "Moderate"; > 10% → red badge "High drift". efficiency_factor → show as number with up/down trend arrow vs. 30-day average (PRD Section 10.4). · Action: Surface both fields on activity detail page. aerobic_decoupling as an interpretive badge; efficiency_factor as a stat tile. Existing activities without values show nothing (null-guard). · DB: columns `aerobic_decoupling NUMERIC(5,2)` and `efficiency_factor NUMERIC(6,4)` added to `rt_activities` (migration: add_derived_metrics_to_rt_activities). · Ready to integrate: yes · Done: Both fields already implemented in activities/[id]/page.jsx — efficiency_factor as stat tile (line 772), aerobic_decoupling as colored badge with thresholds (line 826); both null-guarded. |

| [PENDING] | 2026-05-29 | 📡 API Contract — Backend → Frontend · Issues: #12, #13, #14 (v1.1 milestone) · Additive changes only — no breaking changes. · (1) GET /api/running/v1/activities/[id] — `activity` object now includes `efficiency_factor_30d_avg` (float 4dp, nullable). Null when <3 data points in last 30 days. Use for trend arrow on efficiency_factor stat tile: TrendingUp (green-500) if current > avg, TrendingDown (red-400) if current < avg, no arrow if either is null. · (2) GET /api/running/v1/activities — each item now includes `estimated_vo2max` (float, nullable) and `efficiency_factor` (float 4dp, nullable). Use these for analytics page charts. · (3) GET /api/running/v1/ai/insights — confirmed for dashboard use: calling without activity_id returns all insights; ?limit=N works (max 50); returns pending status items; shape: { id, insight_type, status, is_valid, title, content, data_refs, acknowledged, created_at }. 401 response now consistent with standard format. · Action: (a) Show trend arrow on efficiency_factor stat tile using efficiency_factor_30d_avg. (b) Use estimated_vo2max and efficiency_factor from activities list for analytics charts. (c) Add fetchInsights(limit) to lib/api/running.js for dashboard AI Coach feed (no activity_id param). · Ready to integrate: yes |

## Signals: Frontend → Tester (UI ready)

| Status                 | Date       | Signal                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ---------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [RESOLVED: 2026-05-17] | 2026-05-17 | ✅ UI Ready — Frontend → Tester · Feature: Pending id signals resolved (logout button, UpdateProductName, DeleteProductName, cy.logout command) · Pages/components changed: (1) app/main/components/Sidebar.jsx — desktop sign-out button id changed from `userMenuSignOut_landingPage` to `logoutBtn`. (2) app/main/inventory/product-name/UpdateProductName.jsx — added `id="productNameField_errorMessage_updateDialog"` on FormMessage, `id="productNameInUseWarning_updateDialog"` on warning div, `id="submitUpdateProductNameBtn_productNamePage"` on submit button, `id="cancelUpdateProductNameBtn_productNamePage"` on cancel button. (3) app/main/inventory/product-name/DeleteProductName.jsx — added `id="deleteProductNameBtn_productNamePage"` on trigger button, `id="deleteProductNameConfirmBtn_productNamePage"` on confirm action button. (4) cypress/support/common/auth/ui-commands.js — added `cy.logout()` command. · New testIds added: `logoutBtn` → desktop sign-out button in Sidebar; `productNameField_errorMessage_updateDialog` → FormMessage for product_name field in update dialog; `productNameInUseWarning_updateDialog` → in-use warning box in update dialog; `submitUpdateProductNameBtn_productNamePage` → Update submit button; `cancelUpdateProductNameBtn_productNamePage` → Cancel button in update dialog; `deleteProductNameBtn_productNamePage` → AlertDialogTrigger delete button (disabled when in use); `deleteProductNameConfirmBtn_productNamePage` → AlertDialogAction confirm delete button. · API endpoints used: POST /api/auth/logout (used by cy.logout()) · Ready for: E2E tests + code review |
| [RESOLVED: 2026-05-17] | 2026-05-17 | ✅ UI Ready — Frontend → Tester · Feature: Product Name P2 — Bulk Status Change (K) + Product Count Badge Navigation (L) · Pages/components changed: (1) app/main/inventory/product-name/list/ProductNamesTable.jsx — added Checkbox column, select-all header checkbox, per-row checkboxes, bulk action bar (Set Active / Set Inactive / Deselect All), auto-deselect on filter/search change, indeterminate header state. Product count badge wrapped in `<button>` when count > 0, navigates to `/main/inventory/product-list?name={encoded}`. (2) lib/api/productName.js — added `bulkUpdateProductNameStatus(items, status)` function. (3) app/main/inventory/product-list/ProductsPage.jsx — added `?name=` URL param support alongside existing `?brand=`. (4) cypress/fixtures/app-constants.json — registered 5 new test IDs under `test_ids.product_name`. · New testIds added: `selectAllNames_productNamePage` → select-all checkbox in table header; `bulkActionBar_productNamePage` → bulk action bar container; `bulkSetActiveBtn_productNamePage` → Set Active button; `bulkSetInactiveBtn_productNamePage` → Set Inactive button; `bulkDeselectAllBtn_productNamePage` → Deselect All button. Dynamic IDs (not pre-registered): `nameCheckbox_{id}_productNamePage` → per-row checkbox; `productCountBadge_{id}_productNamePage` → product count badge button. · API endpoints used: PUT /api/inventory/v1/product-name/update/[id] (looped per selected item via bulkUpdateProductNameStatus). · Ready for: E2E tests + code review                                                                                                      |

| [RESOLVED: 2026-05-27] | 2026-05-25 | ✅ UI Ready — Frontend → Tester · Feature: Running Tracker — Dashboard YtdStats + NextRace standalone cards · Pages/components changed: (1) app/main/running/(app)/dashboard/components/TrainingLoad.jsx — removed inline YTD totals and next race sections (moved to dedicated components). (2) app/main/running/(app)/dashboard/components/YtdStats.jsx — new standalone card; receives `ytd_stats` prop from `dashboardData.training_load`; shows total runs, distance, moving time, elevation in a 2×4 responsive grid; shows achievement count when > 0; renders nothing if `distance_m === 0` or no data. (3) app/main/running/(app)/dashboard/components/NextRace.jsx — new standalone card; receives `next_race_goal` prop; shows race distance label, target date, urgency badge (race week / ≤30 days / normal); renders a countdown progress bar for goals ≤90 days away; renders nothing if no goal. (4) app/main/running/(app)/dashboard/page.jsx — imports and renders YtdStats and NextRace after TrainingLoad. (5) lib/inngest/functions/_.js — fixed all 7 Inngest functions from old 3-arg API to v4 triggers-in-config signature (pre-existing build breakage). · New testIds registered in app-constants.json: `ytdStatsCard` (section root in YtdStats), `nextRaceCard` (section root in NextRace). · API data source: both components consume fields already in `dashboardData.training_load` — no new endpoint needed. · Ready for: E2E tests · Done: Acknowledged — YtdStats and NextRace are covered by the dashboard-ui-extended.cy.js suite; no new spec file required for this signal. |
| [RESOLVED: 2026-05-27] | 2026-05-24 | ✅ UI Ready — Frontend → Tester · Feature: Running Tracker — Activities list, Activity detail, Dashboard enhancements (power + gear + temp) · Pages/components changed: (1) app/main/running/(app)/dashboard/components/ActivitySection.jsx — recent activity rows now show power (device_watts only), temperature with color badge, gear name on line 4. (2) app/main/running/(app)/dashboard/components/PerformanceTrends.jsx — added Power tab with PowerChart (Norm W + Avg W lines, purple). (3) app/main/running/(app)/activities/components/RouteMap.jsx — new Leaflet map component, must always be loaded via next/dynamic ssr:false. (4) app/main/running/(app)/activities/page.jsx — full activities list with pagination, type filter from URL, RouteMap thumbnails, activity cards. (5) app/main/running/(app)/activities/[id]/page.jsx — full activity detail with RouteMap, stats grid, secondary stats (power/temp/calories), gear row. · New testIds added: `activitiesPage` (activities list root), `activitiesLoadingSkeleton`, `activitiesError`, `activitiesList`, `activityDetailPage` (detail root). · API endpoints used: GET /api/running/v1/activities (paginated, type filter), GET /api/running/v1/activities/[id], GET /api/running/v1/performance-trends (power fields). · Ready for: E2E tests · Done: Created cypress/e2e/running/activities/activities-api.cy.js (8 tests) and cypress/e2e/running/activities/activities-ui.cy.js (26 tests). |
| [RESOLVED: 2026-05-23] | 2026-05-23 | ✅ UI Ready — Frontend → Tester · Feature: Running Tracker Dashboard · Pages/components changed: app/main/running/(app)/dashboard/page.jsx (full implementation), lib/api/running.js (added getDashboard), cypress/fixtures/app-constants.json (added running_dashboard test IDs) · IDs added: dashboardPage, weeklyStatsCard, trainingLoadCard, activityCalendarCard, recentActivitiesCard, aiCoachCard, healthCheckinCard, dashboardSkeleton, dashboardError · API used: GET /api/running/v1/dashboard · Ready for: E2E tests · Done: Created cypress/e2e/running/dashboard/dashboard-api.cy.js (8 tests) and cypress/e2e/running/dashboard/dashboard-ui.cy.js (22 tests) |
| [RESOLVED: 2026-05-26] | 2026-05-25 | ✅ UI Ready — Frontend → Tester · Feature: Running Tracker — Race Log + Edit Race Goal · Pages/components changed: (1) app/main/running/(app)/race-log/page.jsx — new page at /main/running/race-log; full CRUD UI: loading skeleton (3 rows), error state with retry, empty state with Medal icon + "Log your first race" CTA, list of race cards ordered by race_date DESC. Each card shows: title, DNF badge, race date, distance label, finish time, avg pace, HR, elevation, positions, notes (truncated), activity link. Edit (pencil) and delete (trash) buttons per card. Add/edit modal with full form: title, race date, distance preset/custom, DNF checkbox, finish time (HH:MM:SS input), HR, elevation, positions, notes. Delete uses AlertDialog with confirm. (2) lib/api/running.js — added fetchRaceLog, createRaceLog, updateRaceLog, deleteRaceLog, updateGoal. (3) app/main/running/(app)/activities/[id]/page.jsx — added "Your Next Race" card at page bottom with editGoalBtn; EditGoalModal for updating goal title/distance/date/description via PATCH /api/running/v1/goals/:id. (4) lib/services/running/dashboard/getDashboardData.js — fetchNextRaceGoal query now selects id, title, description in addition to existing fields. (5) app/main/running/(app)/dashboard/components/NextRace.jsx — "View all races" link updated to /main/running/race-log. · testIds used: raceLogPage, raceLogLoadingSkeleton, raceLogError, raceLogEmptyState, raceLogList, raceLogCard, addRaceBtn, raceLogFormModal, raceLogSaveBtn, raceLogDeleteBtn, raceLogDeleteConfirmBtn, editGoalBtn, editGoalModal, editGoalSaveBtn. · API endpoints used: GET /api/running/v1/race-log, POST /api/running/v1/race-log, PATCH /api/running/v1/race-log/:id, DELETE /api/running/v1/race-log/:id, PATCH /api/running/v1/goals/:id, GET /api/running/v1/dashboard. · Ready for: E2E tests · Done: cypress/e2e/running/race-log/race-log-api.cy.js (25 tests covering GET/POST/PATCH/DELETE + auth) and cypress/e2e/running/race-log/race-log-ui.cy.js (34 tests covering all UI states, CRUD modals, delete confirm, mobile) |
| [RESOLVED: 2026-05-26] | 2026-05-26 | ✅ UI Ready — Frontend → Tester · Feature: Running Tracker — HR Zones Chart + AI Insight Card · Pages/components changed: (1) app/main/running/(app)/activities/components/HrZonesChart.jsx — NEW: horizontal CSS bar chart showing Z1-Z5 HR zone distribution; takes `zones` prop from activity; empty state when zones null; data-testid: hrZonesSection, hrZonesEmpty. (2) app/main/running/(app)/activities/components/AIInsightCard.jsx — NEW: AI coach card with purple-400 left border; 5 states: loading skeleton, empty+generate button, pending+refresh, content (markdown rendered), error+retry; calls fetchActivityInsight on mount + requestInsightGeneration on demand; no auto-polling; data-testid: aiInsightCard, aiInsightLoading, aiInsightEmpty, aiInsightGenerateBtn, aiInsightPending, aiInsightContent, aiInsightError, aiInsightRetry. (3) app/main/running/(app)/activities/[id]/page.jsx — MODIFIED: imports HrZonesChart + AIInsightCard; renders AIInsightCard after notes/weather section (before photos); renders HrZonesChart after StreamCharts (at bottom of content). · API: GET /api/running/v1/ai/insights?activity_id=&type=post_activity, POST /api/running/v1/ai/insights/generate · New endpoints registered in app-constants.json: running_ai_insights.list, running_ai_insights.generate · New test IDs in app-constants.json: hrZonesSection, hrZonesEmpty, aiInsightCard, aiInsightLoading, aiInsightEmpty, aiInsightGenerateBtn, aiInsightPending, aiInsightContent, aiInsightError, aiInsightRetry · Ready for: E2E tests |
| [RESOLVED: 2026-05-27] | 2026-05-25 | ✅ UI Ready — Frontend → Tester · Feature: Running Tracker — Shoe Rotation (Gear Management) · Done: Gear API and UI tests already exist in cypress/e2e/running/dashboard/gear-api.cy.js and gear-ui.cy.js; this signal confirmed as covered. · Pages/components changed: (1) lib/api/running.js — added `fetchGear()` (GET /api/running/v1/gear) and `updateGear(id, payload)` (PATCH /api/running/v1/gear). (2) app/main/running/(app)/dashboard/components/ShoeRotation.jsx — new component; fetches gear list on mount; shows active + retired shoes split; each card shows name, brand/model, distance km, mileage progress bar toward retirement_km, category badge, "Nearing limit" alert at ≥90%; inline edit form for category (pill toggle from CATEGORY_OPTIONS) and retirement_km (number input), validated via updateGearSchema; PATCH on save, optimistic local update via onUpdate callback; loading skeleton (3 rows), error state with retry, empty state. (3) app/main/running/(app)/dashboard/page.jsx — ShoeRotation rendered between PerformanceTrends and AiCoachPlaceholder. · testIds used (from app-constants.json running_gear._): `gearPage` on section root, `gearLoadingSkeleton` on skeleton wrapper, `gearError` on error container, `gearList` on active shoe `<ul>`, `gearCard` on each `<li>`, `gearSaveBtn` on save button in edit form. · API endpoints used: GET /api/running/v1/gear, PATCH /api/running/v1/gear. · Ready for: E2E tests |
| [RESOLVED: 2026-05-27] | 2026-05-27 | ✅ UI Ready — Frontend → Tester · Feature: Running Tracker — Analytics Page · Pages/components changed: (1) app/main/running/(app)/analytics/page.jsx — new full-featured Analytics page at /main/running/analytics; replaces placeholder stub; self-contained 'use client' component with 5 chart sections + summary stats row. (2) cypress/fixtures/app-constants.json — added route `running_analytics` + test_ids section `running_analytics` with 7 IDs. · Sections rendered: Summary stats row (total runs, distance, avg weekly km, best pace); Weekly Distance (12-week bar chart using fetchActivities limit=200); Pace Trend (LineChart + 3-run moving avg from fetchPerformanceTrends); Best Pace by Distance (bar chart per distance bracket); Training Load ACWR (gauge + acute/chronic bars from getDashboard); Race Predictor (Riegel formula, source bracket toggleable). · New IDs added: `analyticsPage` → page root; `analyticsLoadingSkeleton` → loading state; `analyticsError` → error state; `weeklyDistanceSection`, `paceTrendSection`, `bestPaceSection`, `trainingLoadSection`, `racePredictorSection` → section roots. · API endpoints used: GET /api/running/v1/activities (limit=200), GET /api/running/v1/performance-trends (limit=50), GET /api/running/v1/dashboard. · Ready for: E2E tests |
| [RESOLVED: 2026-05-17] | 2026-05-17 | ✅ UI Ready — Frontend → Tester · Feature: Product History — P0 gap resolved (all 13 testIds added, search + sort + skeleton + empty state implemented) · Pages/components changed: (1) app/main/inventory/product-history/ProductHistoryPageClient.jsx — added searchQuery + sortOption state, search input (`id="searchInput_productHistoryPage"`), clear search button (`id="clearSearchBtn_productHistoryPage"`), loading skeleton (`id="loadingSkeleton_productHistoryPage"`, 7 cols × 5 rows), true empty state (`id="emptyState_productHistoryPage"`, PackageOpen icon + "No usage history yet"). (2) app/main/inventory/product-history/ProductHistoryTable.jsx — added `id="productHistoryTable_productHistoryPage"` on table, accepts searchQuery + sortOption + onClearFilters props, client-side search (product name substring case-insensitive AND with status filter), 4 sort options (date_desc default, date_asc, name_asc, name_desc), filtered empty state with "Clear filters" button. (3) app/main/inventory/product-history/component/ProductHistoryFilterDropdown.jsx — replaced Filter icon with SlidersHorizontal, added `id="filterSortBtn_productHistoryPage"` on trigger, added filter option IDs (active/inactive/completed), added Sort section with 4 sort option IDs, accepts sortOption + onSortChange props, active count badge dot. (4) cypress/fixtures/app-constants.json — added test_ids.product_history section with 13 IDs. · New testIds: `productHistoryTable_productHistoryPage`, `searchInput_productHistoryPage`, `clearSearchBtn_productHistoryPage`, `filterSortBtn_productHistoryPage`, `filterOption_active_productHistoryPage`, `filterOption_inactive_productHistoryPage`, `filterOption_completed_productHistoryPage`, `sortOption_date_desc_productHistoryPage`, `sortOption_date_asc_productHistoryPage`, `sortOption_name_asc_productHistoryPage`, `sortOption_name_desc_productHistoryPage`, `emptyState_productHistoryPage`, `loadingSkeleton_productHistoryPage`. · API: GET /api/inventory/v1/product-history/list (server-side fetch in page.jsx). · Ready for: E2E tests + code review |

---

## Signals: Tech Debt / Refactor (low priority)

| Status                 | Date       | Signal                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ---------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [RESOLVED: 2026-05-29] | 2026-05-27 | 🔧 Tech Debt — Reusable Skeleton Component · Current state: `RaceSkeletonRow` di `race-log/page.jsx` dan skeleton rows serupa di beberapa halaman lain didefinisikan inline masing-masing. · Proposal: Buat 1 shared component (e.g. `components/ui/SkeletonRow.jsx` atau `app/main/components/SkeletonRow.jsx`) yang reusable di mana saja. · Priority: LOW — tidak urgent, bisa dikerjakan saat refactor pass berikutnya. · Done: Created `app/main/components/TableSkeletonRows.jsx` — accepts `rows` and `metricWidths` props; first col always renders circle+text skeleton, remaining cols render width-specific cells. Replaced inline `RaceSkeletonRow` in race-log/page.jsx and `SkeletonRow` in activities/page.jsx with `<TableSkeletonRows />`. |

---

## Signals: PM → Any Agent (PRD change impact)

> New signal added 2026-05-27 — see bottom of this section.

| Status                 | Date       | Target        | Signal                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ---------------------- | ---------- | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [RESOLVED: 2026-05-10] | 2026-05-10 | UI/UX Agent   | Product List analysis complete. Design review selesai, PRD v1.8 sudah diupdate dengan spec lengkap.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| [RESOLVED: 2026-05-17] | 2026-05-17 | Tester Agent  | PRD v1.15 — Product Name P0 gaps are now implemented. All 6 gaps resolved: (1) product_count on list endpoint, (2) uniqueness check on create (409 inline error), (3) uniqueness check on update (409 inline error, excludes self), (4) delete guard (409 with count), (5) toast.error on delete failure, (6) warning box + disabled delete button in edit modal when product_count > 0. Please write Cypress tests covering these acceptance criteria. Refer to PRD section 3.1.3 for full spec. Note: Tester → Frontend signals from 2026-05-17 are also pending — the missing test IDs for UpdateProductName and DeleteProductName need to be resolved by Frontend before tests can be written.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| [RESOLVED: 2026-05-27] | 2026-05-25 | Tester Agent  | PRD v2.0 — Section 12 Gear Management (Shoe Rotation) is now documented. Feature is fully implemented: GET /api/running/v1/gear, PATCH /api/running/v1/gear, ShoeRotation.jsx component in dashboard. Please write Cypress E2E tests covering: (1) API — GET gear returns list ordered active first; PATCH updates category and retirement_km; 400 on invalid body; 401 on unauthenticated. (2) UI — loading skeleton shows then resolves; active/retired split renders correctly; edit form opens inline, saves with optimistic update, shows error on failure, closes on cancel; "Nearing limit" badge appears at ≥90% of retirement_km; progress bar color changes at 70% and 90%; empty state shows when no gear synced; error state shows "Try again" button. Test IDs: gearPage, gearLoadingSkeleton, gearError, gearList, gearCard, gearSaveBtn. Refer to PRD Section 12 for full acceptance criteria. · Done: gear-api.cy.js and gear-ui.cy.js already exist in cypress/e2e/running/dashboard/.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| [RESOLVED: 2026-05-25] | 2026-05-25 | UI/UX Agent   | PRD v2.1 — Section 13 Race Log is new and needs design. Key screens: (1) /running/race-log page — list of past races (card layout), loading/empty/error states, add/edit modal, delete confirmation dialog. (2) Activity page — "Edit race goal" button + modal (pre-fills title, distance, date, description from rt_goals). Refer to PRD Section 13.5 and 13.6 for UI states, card layout, form fields, and component spec. Test IDs are defined in Section 13.9. · Done: Frontend built race-log page and activity edit-goal modal directly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| [RESOLVED: 2026-05-25] | 2026-05-25 | Backend Agent | PRD v2.1 — Section 13 Race Log needs backend implementation. Required: (1) DB migration: `ALTER TABLE rt_goals ADD COLUMN title TEXT, ADD COLUMN description TEXT`. (2) New table: `rt_race_log` with schema defined in PRD Section 13.3. (3) New endpoints: GET /api/running/v1/race-log, POST /api/running/v1/race-log, PATCH /api/running/v1/race-log/:id, DELETE /api/running/v1/race-log/:id. (4) Updated endpoint: PATCH /api/running/v1/goals/:id — must now accept title, description in addition to existing target fields. (5) Zod schema in schemas/raceLog.js. Server-side: avg_pace_sec_per_km is computed server-side on insert/update (not sent from client). All endpoints require auth + ownership check. Refer to PRD Section 13.3 and 13.4 for full spec. · Done: All 5 items completed. Migration applied. Files created — see signal below.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| [RESOLVED: 2026-05-28] | 2026-05-27 | Tester Agent  | 📋 PRD v2.5 — Section 13 Race Log post-delivery validation: 4 test ID gaps found between app-constants.json and rendered HTML. (1) `raceLogLoadingSkeleton` — no wrapping ID on skeleton rows in the table; tests using `[id="raceLogLoadingSkeleton"]` will not find element. (2) `raceLogList` — rendered as `id="raceLogTable"` not `raceLogList`; all `IDS.list` selectors will fail. (3) `raceLogCard` — no ID on `<TableRow>` elements; all `IDS.card` selectors will fail. (4) `raceLogDeleteBtn` — delete only exists on detail page (`id="deleteRaceBtn_raceDetailPage"`), not on list rows. Impacted test file: `cypress/e2e/running/race-log/race-log-ui.cy.js`. Recommended path: request Frontend to add missing IDs to `page.jsx` (skeleton wrapper, table container rename, per-row ID, or adjust test selectors to match actual rendered structure). Do NOT modify PRD acceptance criteria to hide these gaps. · Done: Reviewed actual rendered HTML. `raceLogLoadingSkeleton` exists as a wrapping div inside a nested table — selector is correct. `raceLogList` is rendered as `id="raceLogList"` on the overflow-x-auto div — selector is correct. `raceLogCard` is on each `<TableRow>` — selector is correct. `raceLogDeleteBtn` / `raceLogDeleteConfirmBtn` are on the detail page only — tests rewritten to test delete from detail page. app-constants.json updated with `detail_page`, `edit_btn`, `edit_modal`, `edit_save_btn`, and `detail` endpoint. race-log-ui.cy.js Sections E, I, J, K rewritten to match actual implementation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| [RESOLVED: 2026-05-26] | 2026-05-26 | PM Agent      | 📋 PM + Researcher gap review — 2026-05-26 · PM Agent and Researcher Agent reviewed current Running Tracker implementation against PRD and running analytics best practices. Key findings to act on: · **Quick wins (data already available, zero new API):** (1) PR count + achievement count badge on activities list — `pr_count` and `achievement_count` are synced from Strava but never surfaced in UI. (2) Cardiac drift indicator on splits — show HR delta between first and last split, no new data needed. · **High-value derived metrics (computable from existing pace+HR streams):** (3) Aerobic Decoupling (Pa:Hr) — `((pace/HR first half) / (pace/HR second half) - 1) * 100`; <5% = good aerobic base, >5% = above threshold; gate to runs >20min with HR data; show as interpretive badge on activity detail. (4) Efficiency Factor (EF) — `avg speed / avg HR`; running economy indicator. (5) Estimated VO2max per run — Daniels formula from HR-pace; display as 30-day rolling average. · **PRD items not yet built:** (6) Entire Analytics page (`/running/analytics`) — 12-week distance trend, PR progression, race predictor, pace vs HR scatter, VO2 max trend; all specified in PRD Section 10.2 but not started. (7) Dashboard AI Coach card — still placeholder (`AiCoachPlaceholder.jsx`), never shows real AI insights. (8) Pre-activity context (energy/sleep from health log) on activity detail — PRD Section 10.3. (9) HR stream chart with zone coloring (Z1-Z5 shading) — current HR chart is plain line only. · **Compute recommendation:** derive Aerobic Decoupling, EF, and VO2max at ingest time (Strava webhook) and store on `rt_activities`, not on render. Gate all derived metrics: skip if duration <20min or HR data missing. · Action needed: PM Agent to update PRD with derived metrics spec and prioritize Analytics page build. · Done: PRD v2.3 — Section 10.4 Derived Metrics added with Aerobic Decoupling, EF, VO2max spec (ingest-time compute); PR count badge + cardiac drift documented as P0 quick wins. |
